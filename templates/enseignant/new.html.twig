{% extends 'base.html.twig' %}

{% block title %}Créer un Enseignant{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="text-center mb-4">Créer un nouvel Enseignant</h1>

        <div class="card shadow p-4">
            {{ include('enseignant/_form.html.twig', { 'attr': {'class': 'form-control'} }) }}
        </div>

        <div class="text-end mt-3">
            <a href="{{ path('app_enseignant_index') }}" class="btn btn-secondary">
                <i class="bi bi-arrow-left"></i> Retour à la liste
            </a>
        </div>
    </div>
{% endblock %}
