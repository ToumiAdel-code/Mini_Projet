{% extends 'base.html.twig' %}

{% block title %}Modifier un Enseignant{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="text-center mb-4">Modifier un Enseignant</h1>

        <div class="card shadow p-4">
            {{ include('enseignant/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>

        <div class="d-flex justify-content-between mt-4">
            <a href="{{ path('app_enseignant_index') }}" class="btn btn-secondary">
                <i class="bi bi-arrow-left"></i> Retour à la liste
            </a>
            {{ include('enseignant/_delete_form.html.twig', { 'attr': { 'class': 'd-inline' } }) }}
        </div>
    </div>
{% endblock %}
