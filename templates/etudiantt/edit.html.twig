{% extends 'base.html.twig' %}

{% block title %}Modifier un Étudiant{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="text-center mb-4">Modifier un Étudiant</h1>

        <div class="card shadow p-4">
            {{ include('etudiantt/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>

        <div class="d-flex justify-content-between mt-4">
            <a href="{{ path('app_etudiantt_index') }}" class="btn btn-secondary">
                <i class="bi bi-arrow-left"></i> Retour à la liste
            </a>
            {{ include('etudiantt/_delete_form.html.twig', { 'attr': { 'class': 'd-inline' } }) }}
        </div>
    </div>
{% endblock %}
