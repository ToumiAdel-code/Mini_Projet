<form method="post" action="{{ path('app_soutenance_delete', {'id': soutenance.id}) }}" onsubmit="return confirm('Êtes-vous sûr(e) de vouloir supprimer cette soutenance ?');" class="d-inline">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ soutenance.id) }}">
    <button class="btn btn-danger">
        <i class="bi bi-trash"></i> Supprimer
    </button>
</form>
