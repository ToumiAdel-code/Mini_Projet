{% extends 'base.html.twig' %}

{% block title %}Créer une nouvelle Soutenance{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="text-center mb-4">Créer une nouvelle Soutenance</h1>

        <div class="card shadow p-4">
            {{ include('soutenance/_form.html.twig', {'button_label': 'Enregistrer'}) }}
        </div>

        <div class="text-end mt-3">
            <a href="{{ path('app_soutenance_index') }}" class="btn btn-secondary">
                <i class="bi bi-arrow-left"></i> Retour à la liste
            </a>
        </div>
    </div>
{% endblock %}
